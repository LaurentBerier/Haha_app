# QA Run 2026-03-19

Goal: verify current app/API voice-conversation reliability behavior after latest mic + greeting orchestration updates and documentation sync.

## Commands Run

1. `npm run typecheck` -> PASS
2. `npm run lint` -> PASS
3. `npm run verify:profile-prompt` -> PASS
4. `npm run test:unit` -> PASS (28 suites, 168 tests)
5. `npm run smoke:auth` -> PASS
6. `npm run smoke:voice` -> PASS (authenticated probe skipped without `SMOKE_AUTH_TOKEN`)
7. `npm run export:web` -> PASS
8. `npm run check:mobile-env` -> PASS with warnings (`Java runtime`, `adb`, `emulator`)

## Notes

- `npm run export:web` completed with generated web bundles and asset export in `dist-web`.
- `npm run smoke:auth` returned expected unauthenticated responses:
  - `claude` no-auth -> `401`
  - `admin-account-type` no-auth -> `401`
  - `delete-account` no-auth -> `401`
  - `payment-webhook` no-auth -> `500` (expected by script contract when secret is missing)
- `npm run smoke:voice` returned expected baseline responses:
  - preflight -> `204`
  - no-auth -> `401`
- iOS Detox e2e was not rerun in this pass (no code changes in test harness in this docs-only sweep).
