# QA Run 2026-03-23

Goal: validate mode-select conversation layout updates (overlay expansion up to mode buttons and removal of duplicate page scrollbar on web).

## Commands Run

1. `npm run typecheck` -> PASS
2. `npx eslint src/app/mode-select/[artistId]/index.tsx` -> PASS

## Notes

- Compact mode-select overlay top now clamps from measured category-grid bottom for both mobile and web fallback paths.
- Background `ScrollView` is now disabled in compact conversation mode to avoid a second page-level scrollbar on the far-right edge.
- Chat message list (`FlatList`) remains independently scrollable as the single intended conversation scroll surface.
