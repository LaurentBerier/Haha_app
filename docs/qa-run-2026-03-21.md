# QA Run 2026-03-21

Goal: validate admin dashboard/menu/access fixes (route visibility, loading-loop prevention, admin API client path normalization).

## Commands Run

1. `npm run typecheck` -> PASS
2. `npx eslint src/services/adminService.ts src/app/admin/index.tsx src/app/admin/users.tsx` -> PASS
3. `npm run typecheck` -> PASS (after auth-bootstrap refactor)
4. `npx eslint src/hooks/useAuth.ts src/app/_layout.tsx src/app/admin/_layout.tsx src/app/settings/index.tsx` -> PASS

## Notes

- Admin menu visibility is now driven from root layout with `isAdmin` gating.
- `useAuth` bootstrap is root-owned only (`useAuth({ bootstrap: true })`) to prevent repeated loading resets on nested routes.
- Admin API client now calls normalized paths (`/admin-stats`, `/admin-users`, `/admin-quota-override`, `/admin-account-type`) on top of resolved backend base URL.
