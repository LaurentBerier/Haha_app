# QA Run 2026-04-14

Goal: validate conversation-mode voice stabilization after STT post-playback recovery hardening and TTS chunk tail-rescue completion updates.

## Commands Run

1. `npm run test:unit -- src/hooks/useVoiceConversation.test.ts src/services/voiceEngine.native.test.ts src/hooks/useChat.sendMessage.integration.test.ts src/hooks/useAudioPlayer.test.ts` -> PASS
2. `npm run smoke:voice` -> PASS

## Notes

- Targeted unit baseline after this run:
  - `4` suites passed
  - `66` tests passed
- `smoke:voice` results:
  - `tts preflight` -> `204`
  - `tts no auth` -> `401`
  - `tts with auth` -> `200`
- Known non-blocking warning in native-voice unit tests remains expected in Jest runtime:
  - `ExpoSpeechRecognition` missing warning from native mock environment.
