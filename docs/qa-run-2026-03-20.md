# QA Run 2026-03-20

Goal: validate current app/API behavior after recent conversation-mode, voice-quota, and stability fixes.

## Commands Run

1. `npm run typecheck` -> PASS
2. `npm run lint` -> PASS
3. `npm run test:unit` -> PASS (29 suites, 183 tests)
4. `npm run verify:profile-prompt` -> PASS
5. `npm run smoke:auth` -> PASS
6. `npm run smoke:voice` -> PASS (`tts with auth -> 200`)

## Notes

- `smoke:auth` returned expected auth-contract statuses:
  - `claude` no-auth -> `401`
  - `admin-account-type` no-auth -> `401`
  - `delete-account` no-auth -> `401`
  - `payment-webhook` no-auth -> `500` (accepted by smoke contract)
- `smoke:voice` validated:
  - preflight -> `204`
  - no-auth -> `401`
  - authenticated probe -> `200`
