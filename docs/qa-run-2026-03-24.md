# QA Run 2026-03-24

Goal: validate the latest mode-select conversation stabilization pass (send binding + rendering reliability) and confirm project-wide regression safety.

## Commands Run

1. `npm run typecheck` -> PASS
2. `npm run lint` -> PASS
3. `npm run test:unit` -> PASS

## Notes

- Mode-select send path is now deterministically bound to a validated target conversation with live store checks and explicit `invalidConversation` handling.
- Mode-select message rendering is hardened for long web conversations:
  - conversation list keyed by active bound conversation id
  - increased render window/batch sizing
  - clipping disabled + virtualization disabled for this inline overlay context
- Dev traces for mode-select now include render progression (`messages_rendered`) in addition to send/rebind events.
- Unit baseline after this pass:
  - `42` suites passed
  - `234` tests passed
