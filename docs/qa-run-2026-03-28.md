# QA Run 2026-03-28

Goal: validate the reliability/performance hardening pass (rate-limit window correctness + web TTS cache lifecycle) and confirm docs refresh safety.

## Commands Run

1. `npm run typecheck` -> PASS
2. `npm run lint` -> PASS
3. `npm run test:unit -- api/__tests__/_utils.rate-limit.test.js api/__tests__/claude.test.js src/services/ttsService.test.ts` -> PASS
4. `npm run test:unit` -> PASS
5. `npm run verify:profile-prompt` -> PASS
6. `npm run smoke:auth` -> PASS
7. `npm run smoke:voice` -> PASS

## Notes

- Unit baseline after this run:
  - `64` suites passed
  - `346` tests passed
- Targeted coverage validates:
  - non-default rate-limit window bucket behavior in shared KV limiter and Claude KV limiter
  - web TTS cache reset and LRU-style eviction/revocation behavior
- Smoke coverage confirms hosted API behavior remains stable:
  - auth-protected endpoints reject unauthenticated calls as expected
  - `/api/tts` preflight and authenticated request succeed under current environment configuration
