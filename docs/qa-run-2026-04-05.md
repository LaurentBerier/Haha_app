# QA Run 2026-04-05

Goal: validate greeting guard imperative-read fix and free tier limit increases.

## Changes Covered

- `src/app/mode-select/[artistId]/index.tsx`: greeting guard now reads `greetedArtistIds` via `useStore.getState()` instead of the reactive `hasArtistBeenGreetedThisSession` selector, preventing `markArtistGreeted()` from triggering a cleanup/re-run cycle that cancelled TTS before audio could play.
- `api/claude.js`: free tier `max_tokens` raised from 200→400 (normal), 180→360 (soft1), 130→280 (soft2); context window raised from 5→12 (normal), 5→10 (soft1), 3→7 (soft2).
- `api/greeting.js`: greeting `max_tokens` raised 160→220 for French output safety margin.
- `src/server/ttsHandler.js`: free tier TTS monthly cap raised from 80→200 000 to match confirmed full-voice access for free tier (decision 2026-03-21).

## Commands Run

1. `npm run typecheck` -> PASS
2. `npm run lint` -> PASS
3. `npm run test:unit` -> PASS

## Notes

- Unit baseline after this run:
  - `99` suites passed
  - `550` tests passed
- Free tier TTS cap of 80 was causing immediate `429 TTS_QUOTA_EXCEEDED` on first greeting, silencing autoplay for all new free users. Now effectively uncapped for practical session use.
