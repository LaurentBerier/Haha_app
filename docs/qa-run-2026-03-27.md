# QA Run 2026-03-27

Goal: run a full regression baseline before publishing the latest code-review and status-document refresh.

## Commands Run

1. `npm run typecheck` -> PASS
2. `npm run lint` -> PASS
3. `npm run test:unit` -> PASS
4. `npm run verify:profile-prompt` -> PASS
5. `npm run smoke:auth` -> PASS
6. `npm run smoke:voice` -> PASS

## Notes

- Unit baseline after this run:
  - `63` suites passed
  - `334` tests passed
- Smoke coverage confirms current hosted API behavior:
  - unauthenticated auth-protected endpoints reject as expected
  - `/api/tts` preflight and authenticated voice request succeed under current environment configuration
