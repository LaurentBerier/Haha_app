# QA Run 2026-04-04

Goal: validate the subscription-cancel resilience fix, lint cleanup, and repository-wide regression baseline.

## Commands Run

1. `npm run typecheck` -> PASS
2. `npm run lint` -> PASS
3. `npm run verify:profile-prompt` -> PASS
4. `npm run test:unit` -> PASS
5. `npm run smoke:auth` -> PASS
6. `npm run smoke:voice` -> PASS

## Notes

- Unit baseline after this run:
  - `99` suites passed
  - `550` tests passed
- Targeted risk coverage included:
  - duplicate stripe-link fallback in subscription cancel path (`api/__tests__/subscription-cancel.test.js`)
  - voice autoplay policy gating (`src/utils/voicePlaybackPolicy.test.ts`)
- Smoke probes passed against production API base (`https://app.ha-ha.ai/api`).
