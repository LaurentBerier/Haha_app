# QA Run 2026-04-01

Goal: validate a full regression baseline before and after documentation alignment updates.

## Commands Run

1. `npm run typecheck` -> PASS
2. `npm run lint` -> PASS
3. `npm run test:unit` -> PASS
4. `npm run verify:profile-prompt` -> PASS
5. `npm run smoke:auth` -> PASS
6. `npm run smoke:voice` -> PASS

## Notes

- Unit baseline after this run:
  - `82` suites passed
  - `442` tests passed
- Smoke coverage against `https://app.ha-ha.ai/api` remained healthy:
  - auth-protected endpoints rejected unauthenticated calls as expected
  - `/api/tts` preflight returned `204`
  - `/api/tts` authenticated probe returned `200` in current environment
